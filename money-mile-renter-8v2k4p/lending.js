(()=>{const actions=document.querySelector('.actions');if(!actions||document.getElementById('familyLendingBtn'))return;const btn=document.createElement('button');btn.id='familyLendingBtn';btn.className='secondary';btn.style.gridColumn='1/-1';btn.textContent='Lend / Return Money';actions.appendChild(btn);const key='mmr8_entries';const get=()=>{try{const v=JSON.parse(localStorage.getItem(key)||'[]');return Array.isArray(v)?v:[]}catch{return[]}};const save=v=>localStorage.setItem(key,JSON.stringify(v));const today=()=>{const d=new Date();return `${d.getFullYear()}-${String(d.getMonth()+1).padStart(2,'0')}-${String(d.getDate()).padStart(2,'0')}`};btn.addEventListener('click',()=>{const returned=confirm('Has money been returned to you?\n\nOK = Money Returned\nCancel = Lend Money');const person=prompt(returned?'Who returned the money?':'Who are you lending the money to?');if(!person||!person.trim())return;const amount=Number(prompt('Amount (£)'));if(!amount||amount<=0)return alert('Enter a valid amount.');const date=prompt('Date (YYYY-MM-DD)',today());if(!date||!/^\d{4}-\d{2}-\d{2}$/.test(date))return alert('Enter the date as YYYY-MM-DD.');const entries=get();entries.push({id:Date.now(),type:returned?'income':'expense',amount,category:returned?'Money Returned':'Money Lent',date,note:(returned?'Returned by ':'Lent to ')+person.trim(),familyPerson:person.trim()});save(entries);location.reload()})})();